-- Enum definitions used by the product/catalog module.

DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'catalog_visibility_status') THEN
        CREATE TYPE catalog_visibility_status AS ENUM ('show', 'hidden');
    END IF;
END
$$;

